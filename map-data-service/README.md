# Map Data Service

Serverless service that provides map data for the Traffic Jam application.

# How to start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
fastapi dev
```
